<div id="primary" class="site-content center-block">
    <div class="breadcrumbs">{{ breadcrumbs }}</div>
    <div id="content" role="main">

        <h1>Новости ИФНС</h1>

        <ul class="page-sections">
            {% for inspection in inspections %}
                <li>
                    <a href="/news-fns/{{ inspection.slug }}">{{ inspection.name }}</a>
                </li>
            {% endfor %}
        </ul>

        {# {% set previous = '' %} #}
        {# {% for new in news %} #}

        {# {% if previous != new.post_date|date("d.m.Y") %} #}
        {# {% if previous != '' %} #}
        {# </div> #}
        {# </div> #}
        {# {% endif %} #}
        {# <div class="block-one-date"> #}
        {# <div class="one-date-news"> #}
        {# <span><i class="fa fa-calendar-o" aria-hidden="true"></i> {{ new.post_date|date("d.m.Y") }}</span> #}
        {# </div> #}
        {# <div class="content-article"> #}
        {# {% endif %} #}
        {# <div class="one-news"> #}
        {# <div class="one-news-title"> #}
        {# <a href="/news_fns/{{ new.slug }}">{{ new.title }}</a> #}
        {# </div> #}
        {# <div>{{ new.content | excerpt(30) }}</div> #}
        {# </div> #}

        {# {% set previous = new.post_date|date("d.m.Y") %} #}

        {# {% endfor %} #}

        {# </div> #}
        {# </div> #}

        {# <nav class="nav-single"> #}
        {# <span class="nav-previous">{% if paginat.prev %}<a href="{{ paginat.prev.link }}">предыдущие</a>{% else %} {% endif %}</span> #}
        {# <span class="nav-next">{% if paginat.next %}<a href="{{ paginat.next.link }}">cледующие</a>{% else %} {% endif%}</span> #}
        {# </nav><!-- .nav-single --> #}


    </div><!-- #content -->
</div><!-- #primary -->