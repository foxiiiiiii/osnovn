{% set previous = '' %}
        {% for new in news %}

            {% if previous != new.post_date|date("d.m.Y") %}
                {% if previous != '' %}
                    </div>
                    </div>
                {% endif %}
<div class="block-one-date">
    <div class="one-date-news">
        <span><i class="calendar-ico"></i> {{ new.post_date|date("d.m.Y") }}</span>
    </div>
    <div class="content-article">
        {% endif %}
        <div class="one-news">
            {% if new.thumbnail %}
                <a href="/news/{{ new.slug }}" title="{{ new.title }}">
                    <img src="{{ new.thumbnail.src|resize(400, 250) }}" alt="{{ new.title }}">
                </a>
            {% endif %}
            <div class="one-news-title">
                {% if fns %}
                    <a href="/news_fns/{{ new.slug }}">{{ new.title }}</a>
                {% else %}
                    <a href="/news/{{ new.slug }}">{{ new.title }}</a>
                {% endif %}
            </div>
            <div>{{ new.content | excerpt(30) }}</div>
        </div>

        {% set previous = new.post_date|date("d.m.Y") %}

        {% endfor %}
    </div>
</div>


